import 'package:flutter/material.dart';

class AppColors {
  // Colores principales solicitados
  static const Color primaryYellow = Color(0xFFFFDE59);
  static const Color primaryBlue = Color(0xFF253B83);

  // Colores secundarios de la imagen
  static const Color secondarySkyBlue = Color(0xFF0090D6);
  static const Color secondaryLime = Color(0xFFD8E022);
  
  // Colores base para la interfaz
  static const Color backgroundLight = Color(0xFFF8F9FA);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFF1C1B1F);
}